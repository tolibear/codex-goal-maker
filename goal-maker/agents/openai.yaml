interface:
  display_name: "Goal Maker"
  short_description: "Prepare a state-machine control plane for long-running Codex goals."
  default_prompt: "Use goal-maker to prepare or repair goal.md, state.yaml, units, evidence, and artifacts, then print the /goal Follow command instead of starting /goal automatically."
policy:
  allow_implicit_invocation: true
